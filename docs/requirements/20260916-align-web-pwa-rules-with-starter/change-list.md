# 改动点：Web/PWA 规则与 create-pwa-app 模板对齐

## 计划改动

| 模块/文件 | 改动内容 | 影响 |
| --- | --- | --- |
| `rules/web-pwa/` 七份文档 | 移除模板重复实现，保留业务开发和验收规则；更新部署、图标与字体约束 | Web/PWA 后续开发规范 |
| `docs/requirements/README.md` 及本需求目录 | 登记、记录方案与结果 | 仓库公共需求追踪 |

## 实际改动

| 模块/文件 | 改动内容 | 与方案差异 |
| --- | --- | --- |
| `rules/web-pwa/README.md` | 改为模板基线与专项规则入口 | 无 |
| `rules/web-pwa/应用布局与运行.md`、`界面与交互.md` | 移除框架实现示例，保留新增业务界面约束 | 无 |
| `rules/web-pwa/本地数据与离线.md`、`路由与发布.md` | 保留业务数据、路由规则；部署改为应用内 `pnpm deploy` 和配置推导 | 无 |
| `rules/web-pwa/图标与静态资源.md`、`验收清单.md` | 移除强制字体与图标库要求，改为按模板和改动范围验收 | 无 |
| `docs/requirements/README.md` 及本需求目录 | 登记、记录方案和实际结果 | 无 |

## 配套变更

- 数据库：无
- API：无
- 配置：无
- 文档：上述规则与需求文档

## 验证结果

- 文档完整性与相对链接检查：通过，七份规则文件齐全，内部链接目标存在。
- 过时要求搜索：通过，规则中不再要求 `npm run build`、仓库根部署脚本或强制 Font Awesome。
- `git diff --check`：通过。
- 对照验收标准：通过。规则说明以 `app.config.ts` 推导 slug、使用应用内 `pnpm deploy`、字体沿用模板，并保留业务 UI、数据和路由验收。

## 遗留事项

- 无。模板未来升级后需重新核对本目录规则。
